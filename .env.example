# Lingueez configuration
# Copy this file to .env and fill in only what you need.
# The plugin loads these values on startup.
#
# You sign in to the built-in Lingueez server from inside the app
# (Configure -> Sign in) — there are no server credentials to set here.

# Optional translation provider
DEEPL_API_KEY=key
